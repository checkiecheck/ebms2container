apiVersion: v1
kind: Service
metadata:
  name: {{ include "cpa-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cpa-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cpa-service.selectorLabels" . | nindent 4 }}
