apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cpa-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cpa-service.labels" . | nindent 4 }}
automountServiceAccountToken: false
