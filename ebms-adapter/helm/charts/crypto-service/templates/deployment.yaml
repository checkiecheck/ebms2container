apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "crypto-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "crypto-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "crypto-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "crypto-service.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
    spec:
      serviceAccountName: {{ include "crypto-service.fullname" . }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: crypto-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "crypto-service.fullname" . }}-config
            - secretRef:
                name: {{ include "crypto-service.fullname" . }}-secret
          {{- if .Values.persistence.enabled }}
          volumeMounts:
            - name: keystore-volume
              mountPath: {{ .Values.persistence.mountPath }}
              readOnly: false
          {{- end }}
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 45
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- if .Values.persistence.enabled }}
      volumes:
        - name: keystore-volume
          persistentVolumeClaim:
            claimName: {{ include "crypto-service.fullname" . }}-keystore
      {{- end }}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              {{- include "crypto-service.selectorLabels" . | nindent 14 }}
